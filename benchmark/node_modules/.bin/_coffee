#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/sh" ]; then
  "$basedir//bin/sh"  "$basedir/../streamline/bin/_coffee" "$@"
  ret=$?
else 
  /bin/sh  "$basedir/../streamline/bin/_coffee" "$@"
  ret=$?
fi
exit $ret
